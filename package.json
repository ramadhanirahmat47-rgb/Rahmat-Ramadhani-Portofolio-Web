{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p public && cp -r assets *.html *.css *.js public/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
